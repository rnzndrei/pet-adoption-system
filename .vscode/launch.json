{
  "version": "0.2.0",
  "configurations": [
    
    {
      "name": "Run C++ in external terminal",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/pet_adoption_system",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": true,
      "MIMode": "gdb",
      "setupCommands": [
        {
          "description": "Enable pretty-printing for gdb",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ],
      "preLaunchTask": "build cpp"
    }
  ]
}